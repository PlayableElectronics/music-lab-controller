# Devices

Device adapters will use stable logical identities and explicit handshakes,
not transient Linux device names alone. Adapters should remain separate from
the transport/session ownership in `labd`.
